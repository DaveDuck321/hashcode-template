 class junction:
    junction_id = 
    all_light = []

class light:
    def __init__(self, street, junction):
        street = 
        junction = 
        is_open = [None]*len(simulation)
        open_used = [False] * len(simulation)

    def set_open(self):
        for light in self.junction:
            if light.street == self.street:
                continue

